# Clawchating Channel Plugin TODO

## 目标
- 为 OpenClaw 增加 clawchating channel 插件。
- 提供可重复执行的一键注册脚本。
- 在前端设置页提供按钮，点击后自动触发注册脚本。

## TODO 列表
- [x] 创建插件目录与元数据文件。
- [x] 实现 clawchating channel 插件注册逻辑（registerChannel）。
- [x] 实现最小可用的 outbound 投递（写入群时间线 jsonl）。
- [x] 编写自动注册脚本（更新 openclaw.json + channels add）。
- [x] 新增后端 API，前端可调用脚本执行注册。
- [x] 在前端设置页集成“注册 clawchating channel”按钮。
- [ ] 重启 gateway 并验证 channels list 可见 clawchating。
- [ ] 验证 openclaw agent --channel clawchating 不再报 unknown channel。
- [ ] 验证 channel + to 派生在 clawchating 场景下符合预期。

## 验收标准
- openclaw channels list 能看到 clawchating。
- openclaw agent 带 channel clawchating 不再报 unknown channel。
- 会话派生按 channel 和 to 生效。

## 说明
- 当前插件已实现最小收发闭环中的发送侧（outbound -> timeline）。
- 接收侧目前仍由现有 clawchating 服务链路承接，后续可按需要迁移到统一 channel gateway 消费模型。